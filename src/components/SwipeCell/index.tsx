import React, { FC } from 'react';
import './index.less';

import { SwipeCellProps } from './interface';

const SwipeCell: FC<SwipeCellProps> = (props) => {
  const {} = props;
  return <div></div>;
};

export default SwipeCell;
